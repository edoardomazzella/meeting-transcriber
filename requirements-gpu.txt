# Meeting Transcriber — CUDA (GPU) dependencies
# Install with: pip install -r requirements-gpu.txt
#
# Installs PyTorch with CUDA 12.1 support.
# For other CUDA versions see: https://pytorch.org/get-started/locally/

--extra-index-url https://download.pytorch.org/whl/cu121
torch
PySide6
faster-whisper
pyannote.audio
soundcard
soundfile
numpy
keyring
